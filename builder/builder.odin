package builder

build_package :: proc(build_path: string) {
	// Walk to expected ark.json file locations. Read each one (can we stream that buffer in?) and if we find multiple configurations, ask which they want to use.
	// Given a UserConfig
	// Check the tool of that version exists on path

}
